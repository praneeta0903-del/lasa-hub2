{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "SPA rewrite — same as workspace-root vercel.json. Whichever Vercel reads (depends on Root Directory project setting), this makes deep links like /admin work instead of 404ing.",
  "rewrites": [
    { "source": "/((?!_expo/|_next/|api/|assets/|favicon\\.ico|robots\\.txt|.*\\..*).*)", "destination": "/" }
  ]
}
